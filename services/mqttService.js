import { Client } from 'paho-mqtt';
import { categories } from '../data/cardsData';

// ...lógica para conectar y recibir mensajes MQTT

export const connectToMQTT = (setMessages) => {
  // ...lógica de conexión MQTT
};
